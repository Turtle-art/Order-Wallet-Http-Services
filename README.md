# Order-Wallet-Http-Services
A lightweight demo showing two microservices — OrderService and WalletService — communicating with each other using synchronous RESTful HTTP calls. The OrderService requests wallet balance information from the WalletService before processing an order, demonstrating basic service-to-service communication
